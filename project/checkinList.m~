function [ bool ] = checkinList( node, list )
%UNTITLED2 Summary of this function goes here
%   This function checkes if a node is in a general list

if node.current.Needle(1,12001)==CLOSED_LIST(i).Needle(1,12001) && node.current.Needle(2,12001)==CLOSED_LIST(i).Needle(2,12001) && node.centre(1,1)==CLOSED_LIST(i).centre(1,1) && node.centre(2,1)==list(i).centre(2,1)
    
end

end

